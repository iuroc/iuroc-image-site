{
    "scripts": {
        "dev": "concurrently \"mvn spring-boot:run\" \"npm run dev\"",
        "build": "vite build",
        "preview": "concurrently \"mvn spring-boot:run\" \"npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "vite": "^5.0.2"
    },
    "dependencies": {
        "bootstrap": "^5.3.2",
        "vanjs-core": "^1.2.6"
    }
}